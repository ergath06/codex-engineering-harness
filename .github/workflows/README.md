# Workflow Templates

This directory is reserved for CI/CD workflow examples.

The initial repository intentionally starts with documentation-oriented checks only. Add stack-specific workflows when reference architectures are implemented.
