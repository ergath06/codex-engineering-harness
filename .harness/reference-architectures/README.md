# Reference Architectures

Reference architectures guide future project work. They are not automatic source of truth.

Use a reference architecture when it matches the approved requirements, risk profile, platform, and team constraints. If a reference conflicts with the approved project direction, document the conflict and resolve it intentionally.

Current placeholders:

- `dotnet-aspnetcore-mvc/`
- `dotnet-maui-blazor-android/`

Add sample code under each `sample-app/` folder when a validated example is available.
